# Strands uses Amazon Bedrock by default. Configure your standard AWS credential
# chain and region before running the `agent` command.
AWS_REGION=us-east-1

# No cloud credentials are required for the deterministic web demo or `inspect`.
